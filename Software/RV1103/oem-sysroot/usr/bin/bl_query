#!/bin/bash

GPIO_PIN=52
VALUE_FILE="/sys/class/gpio/gpio${GPIO_PIN}/value"

if [ -r "$VALUE_FILE" ]; then
    val=$(cat "$VALUE_FILE" 2>/dev/null | tr -d '[:space:]')
    if [ "$val" = "0" ] || [ "$val" = "1" ]; then
        echo "$val"
        exit 0
    fi
fi

echo "0"
exit 0

